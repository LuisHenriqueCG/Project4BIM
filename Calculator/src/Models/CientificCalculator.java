package Models;

public class CientificCalculator extends Calculator {
	double result;
	
	public double raiz (double n){
		return result = Math.sqrt(n);
	}
	public double potencia (double base, double expoente) {
		return result = Math.pow(base, expoente);
	}
}
